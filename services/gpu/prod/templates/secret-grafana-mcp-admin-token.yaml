apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-mcp-admin-token
  namespace: default
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-login
    kind: SecretStore
  target:
    name: grafana-mcp-admin-token
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: token
      remoteRef:
        key: '{{ index .Values "bitwardenIds" "grafana-mcp-admin-token" }}'
        property: password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
