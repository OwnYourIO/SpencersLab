apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ (index .Values `app-template` `global` `nameOverride`) | default .Chart.Name }}-data"
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
