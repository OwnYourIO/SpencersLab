apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ha-mcp
  namespace: default
spec:
  refreshInterval: 1h
  target:
    name: ha-mcp
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        HA_TOKEN: "{{ `{{ .token }}` }}"
  data:
    - secretKey: token
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: SecretStore
      remoteRef:
        key: '{{ index .Values "bitwardenIds" "ha-mcp" }}'
        property: token
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
