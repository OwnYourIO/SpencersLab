global:
  nameOverride: jellyfin

bitwardenIds:
  samba-users: OVERRIDE_NEEDED

app-template:
  controllers:
    jellyfin:
      pod:
        #nodeSelector:
        #  intel.feature.node.kubernetes.io/gpu: "true"
        securityContext:
          runAsNonRoot: true
          runAsUser: 568
          # Using gid 1000 as "shared" group.
          runAsGroup: 1000
          fsGroup: 1000
          fsGroupChangePolicy: OnRootMismatch
          seccompProfile: { type: RuntimeDefault }
          supplementalGroups:
            - 44 # video
            - 109 # render
            - 568
      containers:
        app:
          image:
            repository: jellyfin/jellyfin
            tag: 10.11.11@sha256:aefb67e6a7ff1debdd154a78a7bbb780fd0c873d8639210a7f6a2016ad2b35db
          env:
            JELLYFIN_PublishedServerUrl: https://jellyfin.local
            # This is needed otherwise Jellyfin flags plugins with old versions as needed a reboot and disables them.
            JELLYFIN_RemoveOldPlugins: "true"
          #securityContext:
          #  allowPrivilegeEscalation: false
          #  readOnlyRootFilesystem: true
          #  capabilities: { drop: ["ALL"] }
          probes:
            liveness: &probes
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /health
                  port: &port 8096
                initialDelaySeconds: 0
                periodSeconds: 10
                timeoutSeconds: 1
                failureThreshold: 3
            readiness: *probes
            startup:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /health
                  port: *port
                initialDelaySeconds: 0
                periodSeconds: 10
                timeoutSeconds: 1
                failureThreshold: 30
          resources:
            requests:
              cpu: 100m
              memory: 1Gi
            limits:
              memory: 8Gi
              #gpu.intel.com/i915: 1
  service:
    app:
      controller: jellyfin
      type: LoadBalancer
      ports:
        http:
          port: *port
  ingress:
    app:
      hosts:
        - host: jellyfin.local
          paths:
            - path: /
              service:
                identifier: app
                port: http
  persistence:
    config:
      existingClaim: jellyfin
    cache:
      type: emptyDir
    transcode:
      type: emptyDir
    media:
      existingClaim: media-shared
      globalMounts:
        - path: /media
