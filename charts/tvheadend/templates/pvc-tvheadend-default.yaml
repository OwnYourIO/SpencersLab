apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tvheadend
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClass: local-path
