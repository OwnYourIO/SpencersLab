apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pg-supabase-mcp-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-login
    kind: SecretStore
  target:
    name: pg-supabase-mcp-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        username: readonly
        password: '{{ `{{ .password }}` }}'
  data:
    - secretKey: password
      remoteRef:
        key: {{ index .Values "bitwardenIds" "mcp-pg-supabase" }}
        property: password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
