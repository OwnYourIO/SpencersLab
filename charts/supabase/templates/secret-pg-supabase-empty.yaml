{{ if .Values.emptySecrets }}
apiVersion: v1
# The expectation is that you'll edit this secret in the live cluster with your values.
kind: Secret
metadata:
  name: pg-supabase-secret
  namespace: default
type: Opaque
{{ end }}
