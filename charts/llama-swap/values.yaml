domain: OVERRIDE_VIA_APPSET

# llama-swap application configuration
config:
  # Health check timeout for model readiness.
  # Generous (1h) on purpose: the first load of a large model includes the
  # -hf download (30B Q8 ~32 GB, 30B BF16 ~61 GB) and llama-server only opens
  # its port once download+load finish. At 500s llama-swap killed the process
  # mid-download ("health check timed out after 8m20s"). Cached loads are fast;
  # this only widens the window for cold downloads.
  healthCheckTimeout: 3600
  performance:
    enable: true
    every: 5s

  # Logging
  logLevel: info
  logToStdout: "proxy"

  # Start port for automatic ${PORT} macro
  startPort: 10001

  # Send loading state to clients
  sendLoadingState: true

  # No API keys required (default allow)
  apiKeys: []

  # Global TTL (0 = never automatically unload)
  globalTTL: 0

  # Graceful timeout when unloading a model
  unloadTimeout: 30

  # Macros for reusable configuration
  macros:
    server-cmd: |
      /app/llama-server
      --port ${PORT}
      --host 0.0.0.0
      --jinja
    threads: |
      -t 10
    moe: |
      -ngl 99 
      -ot "\.ffn_.*_exps\.=CPU" 
      --no-mmap
    flash-and-q8: |
      --flash-attn on
      --cache-type-k q8_0
      --cache-type-v q8_0

    ctx-256k: |
      --ctx-size 262144
      -b 2048 
      -ub 2048
    big-ub: |
      -b 2048 
      -ub 2048


    # Trades throughput for fitting onto the 8GB card.
    ctx-1m: |
      -c 1010000 
      --rope-scaling yarn --rope-scale 4 --yarn-orig-ctx 262144
      -b 512 -ub 512

  # Model definitions
  # Models are downloaded automatically from HuggingFace via the -hf flag
  # and cached on the models PVC (LLAMA_CACHE=/models).
  models:
    qwen3.6-27b-q6:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3.6-27B-GGUF:Q6_K
        ${ctx-256k}
        ${flash-and-q8}
      name: "Qwen 3.6 27B (Q6_K)"
      description: "Qwen 3.6 27B general purpose model, Q6_K quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3.6-27b-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3.6-27B-GGUF:Q8_0
        ${flash-and-q8}
        ${ctx-256k}
      name: "Qwen 3.6 27B (Q8_0)"
      description: "Qwen 3.6 27B general purpose model, Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3-coder-next-q6:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-Coder-Next-GGUF:Q6_K
        ${ctx-256k}
        ${flash-and-q8}
      name: "Qwen 3 Coder Next (Q6_K)"
      description: "Qwen 3 Coder Next coding model, Q6_K quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3-coder-next-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-Coder-Next-GGUF:Q8_0
        ${ctx-256k}
        ${flash-and-q8}
      name: "Qwen 3 Coder Next (Q8_0)"
      description: "Qwen 3 Coder Next coding model, Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3.6-35b-a3b-q6:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3.6-35B-A3B-GGUF:UD-Q6_K
        ${ctx-256k}
        ${flash-and-q8}
      name: "Qwen 3.6 35B A3B (UD-Q6_K)"
      description: "Qwen 3.6 35B with 3B active parameters (MoE), UD-Q6_K quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3.6-35b-a3b-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3.6-35B-A3B-GGUF:Q8_0
        ${ctx-256k}
        ${moe}
        ${flash-and-q8}
      name: "Qwen 3.6 35B A3B (Q8_0)"
      description: "Qwen 3.6 35B with 3B active parameters (MoE), Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0
    qwen3.6-35b-a3b-1m:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3.6-35B-A3B-GGUF:Q8_0
        ${ctx-1m}
        ${moe}
        ${flash-and-q8}
      name: "Qwen 3.6 35B A3B (Q8_0) - 1M"
      description: "Qwen 3.6 35B with 3B active parameters (MoE), Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0
    qwen3.6-35b-a3b-bf16:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3.6-35B-A3B-GGUF:BF16
        ${ctx-256k}
        ${moe}
        ${flash-and-q8}
      name: "Qwen 3.6 35B A3B (BF16)"
      description: "Qwen 3.6 35B with 3B active parameters (MoE), BF16 (no quant)"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3-30b-a3b-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-30B-A3B-Instruct-2507-GGUF:Q4_K_M
        ${ctx-256k}
        ${moe}
        ${flash-and-q8}
      name: "Qwen 3 30B A3B Instruct (Q4_K_M)"
      description: "Qwen 3 30B with 3B active parameters (MoE), Q4_K_M quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3-30b-a3b-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-30B-A3B-Instruct-2507-GGUF:Q8_0
        ${ctx-256k}
        ${moe}
        ${flash-and-q8}
      name: "Qwen 3 30B A3B Instruct (Q8_0)"
      description: "Qwen 3 30B with 3B active parameters (MoE), Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3-8b-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-8B-GGUF:Q4_K_M
        ${ctx-256k}
        ${flash-and-q8}
      name: "Qwen 3 8B (Q4_K_M)"
      description: "Qwen 3 8B dense model, Q4_K_M quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3-8b-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-8B-GGUF:Q8_0
        ${ctx-256k}
        ${flash-and-q8}
      name: "Qwen 3 8B (Q8_0)"
      description: "Qwen 3 8B dense model, Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    home-3b-v3-gpu:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf acon96/Home-3B-v3-GGUF:Q8_0
        -ngl 99
        -c 8192
      name: "Home 3B v3 (Q8_0) - GPU"
      description: "Home Assistant 3B v3 model on GPU, Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    kat-coder-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf bartowski/Kwaipilot_KAT-Coder-V2.5-Dev-GGUF:Q4_K_M
        ${ctx-256k}
        ${moe}
        ${flash-and-q8}
      name: "KAT Coder V2.5 Dev (Q4_K_M)"
      description: "Kwaipilot KAT-Coder V2.5 Dev coding model (MoE), Q4_K_M quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    kat-coder-q6:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf bartowski/Kwaipilot_KAT-Coder-V2.5-Dev-GGUF:Q6_K
        ${ctx-256k}
        ${moe}
        ${flash-and-q8}
      name: "KAT Coder V2.5 Dev (Q6_K)"
      description: "Kwaipilot KAT-Coder V2.5 Dev coding model (MoE), Q6_K quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    kat-coder-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf bartowski/Kwaipilot_KAT-Coder-V2.5-Dev-GGUF:Q8_0
        ${ctx-256k}
        ${moe}
        ${flash-and-q8}
      name: "KAT Coder V2.5 Dev (Q8_0)"
      description: "Kwaipilot KAT-Coder V2.5 Dev coding model (MoE), Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0

    home-3b-v3:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf acon96/Home-3B-v3-GGUF:Q4_K_M
        -ngl 0
        -c 8192
      name: "Home 3B v3 (Q4_K_M) - CPU"
      description: "Home Assistant 3B v3 model on CPU, Q4_K_M quant; runs in parallel with GPU models"
      proxy: http://localhost:${PORT}
      ttl: 0

    qwen3-vl-4b-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-4B-Instruct-GGUF:Q4_K_M
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 4B Instruct (Q4_K_M)"
      description: "Vision-language model for Immich image analysis, Q4_K_M quant"
      # Back-compat for consumers configured before quant suffixes were added.
      aliases:
        - qwen3-vl-4b
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-8b-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-8B-Instruct-GGUF:Q4_K_M
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 8B Instruct (Q4_K_M)"
      description: "Vision-language model for Immich image analysis, Q4_K_M quant"
      # Back-compat for consumers configured before quant suffixes were added.
      aliases:
        - qwen3-vl-8b
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-8b-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-8B-Instruct-GGUF:Q8_0
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 8B Instruct (Q8_0)"
      description: "Vision-language model for Immich image analysis, Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-8b-bf16:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-8B-Instruct-GGUF:BF16
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 8B Instruct (BF16)"
      description: "Vision-language model for Immich image analysis, BF16 (no quant)"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-30b-a3b-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-30B-A3B-Instruct-GGUF:Q4_K_M
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        ${moe}
        -c 16384
      name: "Qwen3 VL 30B A3B Instruct (Q4_K_M)"
      description: "Vision-language MoE model for Immich image analysis, Q4_K_M quant, CPU-offloaded experts"
      # Back-compat for consumers configured before quant suffixes were added.
      aliases:
        - qwen3-vl-30b-a3b
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-30b-a3b-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-30B-A3B-Instruct-GGUF:Q8_0
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        ${moe}
        -c 16384
      name: "Qwen3 VL 30B A3B Instruct (Q8_0)"
      description: "Vision-language MoE model for Immich image analysis, Q8_0 quant, CPU-offloaded experts"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-30b-a3b-bf16:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-30B-A3B-Instruct-GGUF:BF16
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        ${moe}
        -c 16384
      name: "Qwen3 VL 30B A3B Instruct (BF16)"
      description: "Vision-language MoE model for Immich image analysis, BF16 (no quant), CPU-offloaded experts"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-2b-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-2B-Instruct-GGUF:Q8_0
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 2B Instruct (Q8_0)"
      description: "Vision-language model for Immich image analysis, Q8_0 quant; faster processing for simpler descriptions"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-2b-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-2B-Instruct-GGUF:Q4_K_M
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 2B Instruct (Q4_K_M)"
      description: "Vision-language model for Immich image analysis, Q4_K_M quant; faster processing for simpler descriptions"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-2b-bf16:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-2B-Instruct-GGUF:BF16
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 2B Instruct (BF16)"
      description: "Vision-language model for Immich image analysis, BF16 (no quant); faster processing for simpler descriptions"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-4b-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-4B-Instruct-GGUF:Q8_0
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 4B Instruct (Q8_0)"
      description: "Vision-language model for Immich image analysis, Q8_0 quant"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-4b-bf16:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-4B-Instruct-GGUF:BF16
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 4B Instruct (BF16)"
      description: "Vision-language model for Immich image analysis, BF16 (no quant)"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-4b-thinking-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-4B-Thinking-GGUF:Q8_0
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 4B Thinking (Q8_0)"
      description: "Reasoning vision-language model for Immich image analysis, Q8_0 quant; good balance of speed and accuracy"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-4b-thinking-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-4B-Thinking-GGUF:Q4_K_M
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 4B Thinking (Q4_K_M)"
      description: "Reasoning vision-language model for Immich image analysis, Q4_K_M quant; good balance of speed and accuracy"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-4b-thinking-bf16:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-4B-Thinking-GGUF:BF16
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        -c 16384
      name: "Qwen3 VL 4B Thinking (BF16)"
      description: "Reasoning vision-language model for Immich image analysis, BF16 (no quant); good balance of speed and accuracy"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-30b-a3b-thinking-q8:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-30B-A3B-Thinking-GGUF:Q8_0
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        ${moe}
        -c 16384
      name: "Qwen3 VL 30B A3B Thinking (Q8_0)"
      description: "Reasoning vision-language MoE model for Immich image analysis, Q8_0 quant, CPU-offloaded experts; higher accuracy for complex images"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-30b-a3b-thinking-q4:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-30B-A3B-Thinking-GGUF:Q4_K_M
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        ${moe}
        -c 16384
      name: "Qwen3 VL 30B A3B Thinking (Q4_K_M)"
      description: "Reasoning vision-language MoE model for Immich image analysis, Q4_K_M quant, CPU-offloaded experts; higher accuracy for complex images"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

    qwen3-vl-30b-a3b-thinking-bf16:
      cmd: |
        ${server-cmd}
        ${threads}
        -hf unsloth/Qwen3-VL-30B-A3B-Thinking-GGUF:BF16
        # mmproj auto-downloads from the -hf repo (b10015 treats hf:// in --mmproj as a literal path)
        ${moe}
        -c 16384
      name: "Qwen3 VL 30B A3B Thinking (BF16)"
      description: "Reasoning vision-language MoE model for Immich image analysis, BF16 (no quant), CPU-offloaded experts; higher accuracy for complex images"
      proxy: http://localhost:${PORT}
      ttl: 0
      capabilities:
        in:
          - text
          - image
        out:
          - text

  # Concurrency matrix (replaces legacy groups routing).
  # One GPU model at a time; homecpu (CPU) may run alongside any GPU model.
  matrix:
    vars:
      q27bq6: qwen3.6-27b-q6
      q27bq8: qwen3.6-27b-q8
      coderq6: qwen3-coder-next-q6
      coderq8: qwen3-coder-next-q8
      q35q6: qwen3.6-35b-a3b-q6
      q35q8: qwen3.6-35b-a3b-q8
      q35bf16: qwen3.6-35b-a3b-bf16
      q35b1m: qwen3.6-35b-a3b-1m
      q30q4: qwen3-30b-a3b-q4
      q30q8: qwen3-30b-a3b-q8
      q8bq4: qwen3-8b-q4
      q8bq8: qwen3-8b-q8
      katq4: kat-coder-q4
      katq6: kat-coder-q6
      katq8: kat-coder-q8
      homegpu: home-3b-v3-gpu
      homecpu: home-3b-v3
      vl2bq8: qwen3-vl-2b-q8
      vl2bq4: qwen3-vl-2b-q4
      vl2bbf: qwen3-vl-2b-bf16
      vl4bq4: qwen3-vl-4b-q4
      vl4bq8: qwen3-vl-4b-q8
      vl4bbf: qwen3-vl-4b-bf16
      vl8bq4: qwen3-vl-8b-q4
      vl8bq8: qwen3-vl-8b-q8
      vl8bbf: qwen3-vl-8b-bf16
      vl30bq4: qwen3-vl-30b-a3b-q4
      vl30bq8: qwen3-vl-30b-a3b-q8
      vl30bbf: qwen3-vl-30b-a3b-bf16
      v4tq8: qwen3-vl-4b-thinking-q8
      v4tq4: qwen3-vl-4b-thinking-q4
      v4tbf: qwen3-vl-4b-thinking-bf16
      v30tq8: qwen3-vl-30b-a3b-thinking-q8
      v30tq4: qwen3-vl-30b-a3b-thinking-q4
      v30tbf: qwen3-vl-30b-a3b-thinking-bf16
    sets:
      main: "(q27bq6 | q27bq8 | coderq6 | coderq8 | q35q6 | q35q8 | q35bf16 | q35b1m | q30q4 | q30q8 | q8bq4 | q8bq8 | katq4 | katq6 | katq8 | homegpu | vl2bq8 | vl2bq4 | vl2bbf | vl4bq4 | vl4bq8 | vl4bbf | vl8bq4 | vl8bq8 | vl8bbf | vl30bq4 | vl30bq8 | vl30bbf | v4tq8 | v4tq4 | v4tbf | v30tq8 | v30tq4 | v30tbf) & homecpu"

app-template:
  global:
    nameOverride: &chartName llama-swap

  controllers:
    llama-swap:
      annotations:
        reloader.stakater.com/auto: "true"
      containers:
        main:
          image:
            repository: ghcr.io/mostlygeek/llama-swap
            tag: v240-vulkan-b10015
          env:
            TZ: Etc/UTC
            # Cache downloaded models (-hf flag) on the models PVC
            LLAMA_CACHE: /models
          probes:
            liveness:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /health
                  port: 8080
            readiness:
              enabled: true
              custom: true
              spec:
                httpGet:
                  path: /health
                  port: 8080
          resources:
            requests:
              cpu: 1000m
              memory: 4Gi
            limits:
              amd.com/gpu: 1
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

  service:
    llama-swap:
      controller: *chartName
      ports:
        http:
          port: 8080

  persistence:
    config:
      type: configMap
      name: llama-swap-config
      globalMounts:
        - path: /app/config.yaml
          subPath: config.yaml
    models:
      existingClaim: *chartName
      globalMounts:
        - path: /models