# CNPG managed-role password for the postgres-immich ToolHive MCP server.
# Username is hardcoded to the role name (readonly); only the password comes
# from Bitwarden. The LOGIN item's username must equal the role name.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pg-immich-mcp-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-login
    kind: SecretStore
  target:
    name: pg-immich-mcp-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        username: readonly
        password: '{{ `{{ .password }}` }}'
  data:
    - secretKey: password
      remoteRef:
        key: '{{ index .Values "bitwardenIds" "mcp-pg-immich" }}'
        property: password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
